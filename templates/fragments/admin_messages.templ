package fragments

templ AdminMessageSuccess(msg string) {
	<div class="p-3 bg-green-100 rounded text-sm">{ msg }</div>
}

templ AdminMessageError(msg string) {
	<div class="p-3 bg-red-100 rounded text-sm">{ msg }</div>
}
